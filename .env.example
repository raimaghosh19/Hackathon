# For local Vercel development only: copy this file to .env.local.
# In Vercel, add this secret in Project Settings → Environment Variables.
# Never use a VITE_ prefix for this key.
OPENAI_API_KEY=your_openai_api_key_here
